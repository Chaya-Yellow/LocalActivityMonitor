# 活动监控器 — 测试用例

> 创建日期：2026-07-21
> 测试框架：xUnit + FluentAssertions
> 数据库：SQLite In-Memory（数据层测试）
> Mock 策略：NSubstitute（接口 Mock）

---

## 一、测试分层策略

```
┌─────────────────────────────────────────┐
│           E5 性能与集成测试              │  ← 端到端、长时间运行
├─────────────────────────────────────────┤
│    E4 统计与报表测试   │  E3 内容解析测试 │  ← 纯逻辑、高覆盖
├───────────────────────┴─────────────────┤
│           E2 追踪引擎测试               │  ← 状态机 + Mock
├─────────────────────────────────────────┤
│           E1 数据层测试                 │  ← 内存 SQLite
└─────────────────────────────────────────┘
```

| 层级 | 覆盖目标 | 运行频率 |
|------|---------|---------|
| E1 数据层 | ≥ 90% | 每次提交 |
| E2 追踪引擎 | ≥ 85% | 每次提交 |
| E3 内容解析 | ≥ 95% | 每次提交 |
| E4 统计报表 | ≥ 90% | 每次提交 |
| E5 性能基准 | N/A（指标达标） | 每日一次 |

---

## 二、E1 数据层测试

### E1-1 数据库初始化

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-001 |
| **模块** | SqliteContext |
| **标题** | 首次启动自动创建所有表和索引 |
| **前置** | 数据库文件不存在 |
| **步骤** | 1. 调用 `SqliteContext.InitializeAsync()` <br>2. 查询 `sqlite_master` 表 |
| **预期** | `activity_events`、`daily_summaries`、`weekly_summaries`、`monthly_summaries`、`settings` 五张表均存在；`idx_events_date`、`idx_events_cat`、`idx_events_proc` 三个索引均存在 |
| **优先级** | P0 |

### E1-2 创建活动事件

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-002 |
| **模块** | ActivityEventRepository |
| **标题** | 插入单条活动记录并返回自增 ID |
| **前置** | 数据库已初始化 |
| **步骤** | 1. 构造 `ActivityEvent` 对象（所有必填字段填充）<br>2. 调用 `InsertAsync()`<br>3. 按 ID 查询 |
| **预期** | 返回 ID > 0；查询出的记录各字段与插入一致 |
| **优先级** | P0 |

### E1-3 批量插入 50 条记录

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-003 |
| **模块** | ActivityEventRepository |
| **标题** | 批量插入 50 条事件，事务保证全部成功 |
| **前置** | 数据库已初始化 |
| **步骤** | 1. 构造 50 条 ActivityEvent（不同的 start_time、process_name、category）<br>2. 调用 `InsertBatchAsync()`<br>3. 查询当天全部记录 |
| **预期** | 查询结果 Count = 50；每条记录的字段正确 |
| **优先级** | P0 |

### E1-4 按日期范围查询

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-004 |
| **模块** | ActivityEventRepository |
| **标题** | 按日期范围筛选当天事件 |
| **前置** | 数据库中有 3 天数据（昨天 5 条、今天 10 条、明天 5 条） |
| **步骤** | 1. 调用 `GetTodayEventsAsync()`<br>2. 调用 `GetByDateAsync(today)` |
| **预期** | 两种方式均返回 10 条，均为今天的记录 |
| **优先级** | P0 |

### E1-5 更新活动事件

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-005 |
| **模块** | ActivityEventRepository |
| **标题** | 更新记录的编辑字段 |
| **前置** | 已插入一条 ActivityEvent |
| **步骤** | 1. 修改 `edited_title` 和 `edited_desc`<br>2. 调用 `UpdateAsync()`<br>3. 重新查询该记录 |
| **预期** | `edited_title` 和 `edited_desc` 已更新；其他字段不变 |
| **优先级** | P1 |

### E1-6 删除活动事件

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-006 |
| **模块** | ActivityEventRepository |
| **标题** | 删除指定 ID 的活动记录 |
| **前置** | 已插入多条 ActivityEvent |
| **步骤** | 1. 调用 `DeleteAsync(id)`<br>2. 按 ID 查询被删除的记录 |
| **预期** | 删除返回 true；按 ID 查询返回 null |
| **优先级** | P1 |

### E1-7 设置项持久化

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-007 |
| **模块** | SettingsRepository |
| **标题** | 设置项的写入和读取 |
| **前置** | 数据库已初始化 |
| **步骤** | 1. 调用 `SetAsync("idle_threshold", "15")`<br>2. 调用 `GetAsync("idle_threshold")` |
| **预期** | 返回 `"15"`；不存在的 key 返回 null |
| **优先级** | P1 |

### E1-8 每日聚合读写

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-008 |
| **模块** | DailySummaryRepository |
| **标题** | 写入和读取每日聚合 |
| **前置** | 数据库已初始化 |
| **步骤** | 1. 构造 DailySummary 对象<br>2. 调用 `SaveAsync(summary)`<br>3. 调用 `GetByDateAsync(today)` |
| **预期** | 读取的聚合数据各字段一致 |
| **优先级** | P1 |

### E1-9 同日聚合覆盖写入

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-DB-009 |
| **模块** | DailySummaryRepository |
| **标题** | 同一天多次写入自动合并（UPSERT） |
| **前置** | 已写入一条 today 的聚合 |
| **步骤** | 1. 再次调用 `SaveAsync()` 用不同的值写入同一天<br>2. 查询当天聚合 |
| **预期** | 只有一条记录，值为最后一次写入的值 |
| **优先级** | P2 |

---

## 三、E2 追踪引擎测试

### E2-1 窗口切换检测

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-001 |
| **模块** | WindowTracker |
| **标题** | 前台窗口从 A 切换到 B 时正确结束旧记录创建新记录 |
| **前置** | Mock `GetForegroundWindow` 依次返回 handle_A → handle_A → handle_B |
| **步骤** | 1. 调用 `Tick()`（handle_A）<br>2. 调用 `Tick()`（handle_A，累计）<br>3. 调用 `Tick()`（handle_B，切换） |
| **预期** | 第 1 步创建记录 A；第 2 步累加 A 的 duration；第 3 步结束 A、创建 B |
| **优先级** | P0 |

### E2-2 空窗口句柄处理

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-002 |
| **模块** | WindowTracker |
| **标题** | 前台返回 `IntPtr.Zero` 时跳过该 tick |
| **前置** | Mock `GetForegroundWindow` 返回 `IntPtr.Zero` |
| **步骤** | 1. 调用 `Tick()` |
| **预期** | 不发生任何写入；当前活动记录不变；无异常抛出 |
| **优先级** | P1 |

### E2-3 空闲检测 — 触发空闲

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-003 |
| **模块** | IdleDetector |
| **标题** | 无输入超过 15 分钟触发空闲事件 |
| **前置** | Mock `GetLastInputInfo` 返回 `tickCount - 16 * 60 * 1000`（16 分钟前） |
| **步骤** | 1. 调用 `CheckIdle()` |
| **预期** | `IsIdle` = true；`OnIdleStateChanged` 事件被触发，参数为 `true` |
| **优先级** | P0 |

### E2-4 空闲检测 — 恢复输入

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-004 |
| **模块** | IdleDetector |
| **标题** | 空闲后有新输入触发恢复事件 |
| **前置** | 处于空闲状态（IsIdle = true） |
| **步骤** | 1. Mock `GetLastInputInfo` 返回当前 tick（刚有输入）<br>2. 调用 `CheckIdle()` |
| **预期** | `IsIdle` = false；`OnIdleStateChanged` 事件被触发，参数为 `false` |
| **优先级** | P0 |

### E2-5 空闲阈值边界测试

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-005 |
| **模块** | IdleDetector |
| **标题** | 14 分钟不触发空闲，16 分钟触发 |
| **前置** | 阈值 = 15 分钟（900000ms） |
| **步骤** | 1. Mock 输入时间为 14 分钟前 → 检查状态<br>2. Mock 输入时间为 16 分钟前 → 检查状态 |
| **预期** | 14 分钟：`IsIdle` = false；16 分钟：`IsIdle` = true |
| **优先级** | P1 |

### E2-6 睡眠事件处理

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-006 |
| **模块** | SleepDetector |
| **标题** | 系统进入睡眠时触发暂停事件 |
| **前置** | SleepDetector 已注册电源通知 |
| **步骤** | 1. 模拟发送 `WM_POWERBROADCAST` + `PBT_APMSUSPEND` 消息<br>2. 检查状态 |
| **预期** | `OnSleepStateChanged` 事件触发，参数为 `true`（睡眠中） |
| **优先级** | P0 |

### E2-7 唤醒事件处理

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-007 |
| **模块** | SleepDetector |
| **标题** | 系统从睡眠唤醒时触发恢复事件 |
| **前置** | 处于睡眠状态 |
| **步骤** | 1. 模拟发送 `WM_POWERBROADCAST` + `PBT_APMRESUMEAUTOMATIC` 消息 |
| **预期** | `OnSleepStateChanged` 事件触发，参数为 `false`（已唤醒） |
| **优先级** | P0 |

### E2-8 崩溃恢复 — 5 分钟内重启

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-008 |
| **模块** | CrashRecoveryService |
| **标题** | 删除标记文件后启动，距上次记录 < 5 分钟，自动补录 |
| **前置** | 数据库已有截止到 2 分钟前的记录；标记文件已被删除 |
| **步骤** | 1. 调用 `CheckAndRecoverAsync()`<br>2. 查询数据库最新记录 |
| **预期** | 新增一条补录记录，`is_crash_recovered` = 1；start_time = 上次记录 end_time；end_time ≈ 当前时间 |
| **优先级** | P1 |

### E2-9 崩溃恢复 — 正常退出

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-009 |
| **模块** | CrashRecoveryService |
| **标题** | 标记文件存在时启动，不做补录 |
| **前置** | 标记文件存在（上一次正常退出） |
| **步骤** | 1. 调用 `CheckAndRecoverAsync()` |
| **预期** | 无新记录写入；数据库内容不变 |
| **优先级** | P1 |

### E2-10 崩溃恢复 — 超过 5 分钟

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-010 |
| **模块** | CrashRecoveryService |
| **标题** | 距上次记录 > 5 分钟，不做补录 |
| **前置** | 数据库最后一条记录在 10 分钟前；标记文件被删除 |
| **步骤** | 1. 调用 `CheckAndRecoverAsync()` |
| **预期** | 无补录记录；从当前时间开始正常记录 |
| **优先级** | P1 |

### E2-11 引擎完整 Tick 周期

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-011 |
| **模块** | ActivityEngine |
| **标题** | 完整 tick：获取窗口 → 分类 → 写入 → 统计 |
| **前置** | 全部依赖均已 Mock |
| **步骤** | 1. 调用 `Start()`<br>2. 等待 3 个 tick（6 秒）<br>3. 调用 `Stop()` |
| **预期** | 数据库中有 3 条记录；event 包含正确的 category 和 process_name |
| **优先级** | P0 |

### E2-12 引擎暂停/恢复

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-012 |
| **模块** | ActivityEngine |
| **标题** | 暂停后停止写入，恢复后继续 |
| **前置** | 引擎正在运行 |
| **步骤** | 1. 调用 `Pause()`<br>2. 等待 2 个 tick<br>3. 调用 `Resume()`<br>4. 等待 2 个 tick<br>5. 查询数据库 |
| **预期** | 暂停期间无新记录；恢复后继续写入；恢复后的记录 `is_continued` = 1 |
| **优先级** | P0 |

### E2-13 空闲延续逻辑

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-ENG-013 |
| **模块** | ActivityEngine |
| **标题** | 空闲恢复后延续上一条记录 |
| **前置** | Mock：IdleDetector 返回空闲 → 15 分钟后恢复输入 |
| **步骤** | 1. 引擎正常运行（窗口 A）<br>2. 触发空闲（15 分钟）<br>3. 恢复输入<br>4. 检查最新记录 |
| **预期** | 恢复后不创建新记录；原记录 A 的 `end_time` 被更新延续；`is_continued` = 1 |
| **优先级** | P0 |

---

## 四、E3 内容解析测试

### E3-1 Chrome 标题解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-001 |
| **模块** | BrowserTracker |
| **标题** | 标准 Chrome 标题提取页面标题和域名 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"GitHub - Google Chrome"` |
| **步骤** | 1. 调用 `Parse("GitHub - Google Chrome")` |
| **预期** | `Title` = `"GitHub"`；`Domain` = `"github.com"` |
| **优先级** | P0 |

### E3-2 Edge 标题解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-002 |
| **模块** | BrowserTracker |
| **标题** | 标准 Edge 标题提取 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"Stack Overflow - Microsoft Edge"` |
| **步骤** | 1. 调用 `Parse("Stack Overflow - Microsoft Edge")` |
| **预期** | `Title` = `"Stack Overflow"`；`Domain` = 包含 `stackoverflow` |
| **优先级** | P0 |

### E3-3 Firefox 标题解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-003 |
| **模块** | BrowserTracker |
| **标题** | 标准 Firefox 标题提取 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"百度一下 - Mozilla Firefox"` |
| **步骤** | 1. 调用 `Parse("百度一下 - Mozilla Firefox")` |
| **预期** | `Title` = `"百度一下"`；`Domain` = `"baidu.com"` |
| **优先级** | P0 |

### E3-4 长标题截断

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-004 |
| **模块** | BrowserTracker |
| **标题** | 超长标题（>200 字符）不崩溃 |
| **前置** | 无 |
| **测试数据** | 构造 300 字符的标题字符串 |
| **步骤** | 1. 调用 `Parse(longTitle + " - Google Chrome")` |
| **预期** | Title 截取前 200 字符；无异常抛出 |
| **优先级** | P2 |

### E3-5 非浏览器标题

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-005 |
| **模块** | BrowserTracker |
| **标题** | 非浏览器窗口标题返回空结果 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"程序.cs - Visual Studio Code"` |
| **步骤** | 1. 调用 `Parse("程序.cs - Visual Studio Code")` |
| **预期** | `Title` = `null`；`Domain` = `null`（BrowserTracker 不匹配则返回空） |
| **优先级** | P1 |

### E3-6 VS Code 文件解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-006 |
| **模块** | FileTracker |
| **标题** | VS Code 标题提取文件名和项目名 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"app.py - myproject - Visual Studio Code"`；进程名 = `code.exe` |
| **步骤** | 1. 调用 `Parse(title, "code.exe")` |
| **预期** | `FileName` = `"app.py"`；`ProjectName` = `"myproject"` |
| **优先级** | P0 |

### E3-7 Photoshop 文件解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-007 |
| **模块** | FileTracker |
| **标题** | PS 标题只提供文件名，通过工作目录补全路径 |
| **前置** | Mock 进程工作目录 = `"D:\设计\官网"` |
| **测试数据** | 窗口标题 = `"首页设计_v3.psd @ 100% (RGB/8)"`；进程名 = `photoshop.exe` |
| **步骤** | 1. 调用 `Parse(title, "photoshop.exe", workingDir)` |
| **预期** | `FileName` = `"首页设计_v3.psd"`；`FilePath` = `"D:\设计\官网\首页设计_v3.psd"` |
| **优先级** | P1 |

### E3-8 终端路径解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-008 |
| **模块** | FileTracker |
| **标题** | CMD 窗口标题提取当前目录 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"cmd.exe - C:\Users\cccha\Code\ActivityMonitor"`；进程名 = `cmd.exe` |
| **步骤** | 1. 调用 `Parse(title, "cmd.exe")` |
| **预期** | `ProjectName` = `"ActivityMonitor"`；`WorkingDir` = `"C:\Users\cccha\Code\ActivityMonitor"` |
| **优先级** | P1 |

### E3-9 远程桌面解析

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-009 |
| **模块** | FileTracker |
| **标题** | 远程桌面连接记录目标地址 |
| **前置** | 无 |
| **测试数据** | 窗口标题 = `"远程桌面连接 - 192.168.1.100"`；进程名 = `mstsc.exe` |
| **步骤** | 1. 调用 `Parse(title, "mstsc.exe")` |
| **预期** | `Detail` 包含 `"192.168.1.100"`；`SubCategory` = `"remote"` |
| **优先级** | P1 |

### E3-10 项目检测 — 含 Git 仓库

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-010 |
| **模块** | ProjectDetector |
| **标题** | 路径含 .git 目录时返回仓库名 |
| **前置** | 在 `C:\Projects\MyApp\.git\` 下创建 .git 目录 |
| **测试数据** | 文件路径 = `"C:\Projects\MyApp\src\main.py"` |
| **步骤** | 1. 调用 `DetectAsync(filePath)` |
| **预期** | 返回 `"MyApp"` |
| **优先级** | P0 |

### E3-11 项目检测 — 不含 Git

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-011 |
| **模块** | ProjectDetector |
| **标题** | 路径不含 .git 时取最后两级文件夹 |
| **前置** | 无 .git 目录 |
| **测试数据** | 文件路径 = `"D:\Design\Website\v3\index.html"` |
| **步骤** | 1. 调用 `DetectAsync(filePath)` |
| **预期** | 返回 `"Website/v3"` 或 `"v3"` |
| **优先级** | P1 |

### E3-12 项目检测 — 空路径

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-012 |
| **模块** | ProjectDetector |
| **标题** | 空路径返回 `"unknown"` |
| **前置** | 无 |
| **测试数据** | 路径 = `""` |
| **步骤** | 1. 调用 `DetectAsync("")` |
| **预期** | 返回 `"unknown"` |
| **优先级** | P1 |

### E3-13 分类器 — 浏览器识别

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-013 |
| **模块** | ActivityCategorizer |
| **标题** | Chrome 进程 → Category.Web |
| **前置** | 无 |
| **测试数据** | `{ ProcessName: "chrome.exe", WindowTitle: "GitHub - Google Chrome" }` |
| **步骤** | 1. 调用 `Classify(event)` |
| **预期** | `Category` = `Category.Web` |
| **优先级** | P0 |

### E3-14 分类器 — 编辑器识别

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-014 |
| **模块** | ActivityCategorizer |
| **标题** | VS Code 进程 → Category.File |
| **前置** | 无 |
| **测试数据** | `{ ProcessName: "code.exe" }` |
| **步骤** | 1. 调用 `Classify(event)` |
| **预期** | `Category` = `Category.File` |
| **优先级** | P0 |

### E3-15 分类器 — 远程工具

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-015 |
| **模块** | ActivityCategorizer |
| **标题** | mstsc 进程 → Category.App + SubCategory.remote |
| **前置** | 无 |
| **测试数据** | `{ ProcessName: "mstsc.exe" }` |
| **步骤** | 1. 调用 `Classify(event)` |
| **预期** | `Category` = `Category.App`；`SubCategory` = `"remote"` |
| **优先级** | P1 |

### E3-16 分类器 — 用户覆盖

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-016 |
| **模块** | ActivityCategorizer |
| **标题** | `user_category` 设置后覆盖自动分类 |
| **前置** | 无 |
| **测试数据** | `{ ProcessName: "chrome.exe", WindowTitle: "bilibili.com", UserCategory: "work" }` |
| **步骤** | 1. 调用 `Classify(event)` |
| **预期** | `WorkTag` = `"work"`（用户覆盖了默认的 unknown） |
| **优先级** | P1 |

### E3-17 关键词提取 — 中英文混合

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-017 |
| **模块** | KeywordExtractor |
| **标题** | 中英文混合标题提取关键词 |
| **前置** | 无 |
| **测试数据** | 标题 = `"Linux 内核编译参数调优 - Google Chrome"` |
| **步骤** | 1. 调用 `Extract(title)` |
| **预期** | 返回 `["Linux", "内核", "编译", "参数", "调优"]`；不含停用词（"的"、"了"、"在" 等） |
| **优先级** | P0 |

### E3-18 关键词提取 — 全停用词

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-018 |
| **模块** | KeywordExtractor |
| **标题** | 标题全部为停用词时返回空列表 |
| **前置** | 无 |
| **测试数据** | 标题 = `"的 了 是 在 就 都"` |
| **步骤** | 1. 调用 `Extract(title)` |
| **预期** | 返回空列表 `[]` |
| **优先级** | P2 |

### E3-19 关键词提取 — 空标题

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-CA-019 |
| **模块** | KeywordExtractor |
| **标题** | 空字符串标题返回空列表 |
| **前置** | 无 |
| **测试数据** | 标题 = `""` |
| **步骤** | 1. 调用 `Extract("")` |
| **预期** | 返回空列表 `[]`；无异常 |
| **优先级** | P1 |

---

## 五、E4 统计与报表测试

### E4-1 按 App 聚合统计

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-001 |
| **模块** | TodayStatsService |
| **标题** | 按 process_name 聚合当天各应用时长 |
| **前置** | 插入当天数据：code.exe 3600s、chrome.exe 1800s、photoshop.exe 1200s |
| **步骤** | 1. 调用 `GetByAppAsync()` |
| **预期** | 返回 3 条：code.exe(3600s, 54.5%)、chrome.exe(1800s, 27.3%)、photoshop.exe(1200s, 18.2%)；总时长 = 6600s |
| **优先级** | P0 |

### E4-2 按项目聚合统计

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-002 |
| **模块** | TodayStatsService |
| **标题** | 按 project 聚合当天各项目时长 |
| **前置** | 插入当天数据：projectA 5400s、projectB 1200s |
| **步骤** | 1. 调用 `GetByProjectAsync()` |
| **预期** | projectA(5400s, 81.8%)、projectB(1200s, 18.2%) |
| **优先级** | P1 |

### E4-3 按域名聚合统计

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-003 |
| **模块** | TodayStatsService |
| **标题** | 按 domain 聚合当天各网站时长 |
| **前置** | 插入当天数据：github.com 2400s、stackoverflow.com 1200s |
| **步骤** | 1. 调用 `GetByDomainAsync()` |
| **预期** | github.com(2400s, 66.7%)、stackoverflow.com(1200s, 33.3%) |
| **优先级** | P1 |

### E4-4 按工作/非工作聚合

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-004 |
| **模块** | TodayStatsService |
| **标题** | 按 work_tag 聚合工作/非工作时长 |
| **前置** | 插入当天数据：work 5400s、break 600s、personal 600s |
| **步骤** | 1. 调用 `GetByWorkTagAsync()` |
| **预期** | work(5400s, 81.8%)、break(600s, 9.1%)、personal(600s, 9.1%) |
| **优先级** | P1 |

### E4-5 日报生成 — 完整内容

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-005 |
| **模块** | MarkdownExporter |
| **标题** | 生成包含全部 6 个章节的日报 Markdown |
| **前置** | 构造全天的模拟数据（有应用、有项目、有网站、有空闲） |
| **步骤** | 1. 调用 `ExportDailyAsync(today, data)` |
| **预期** | 输出字符串包含：# 工作日报、今日概览、⏱ 时间线、📁 项目分布、📈 应用分布、🌐 网页分类、📝 手动补充 所有章节标题；时间线内包含活动条目 |
| **优先级** | P0 |

### E4-6 日报生成 — 空数据

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-006 |
| **模块** | MarkdownExporter |
| **标题** | 当天无活动记录时生成空日报 |
| **前置** | 当天数据为空 |
| **步骤** | 1. 调用 `ExportDailyAsync(today, emptyData)` |
| **预期** | 输出 Markdown 包含"今日无记录"提示；不抛出异常 |
| **优先级** | P2 |

### E4-7 日报构建器数据一致性

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-007 |
| **模块** | ReportBuilder |
| **标题** | Distribution 数据与原始记录总和一致 |
| **前置** | 插入 20 条跨 3 个项目、4 个应用、5 个域名的记录 |
| **步骤** | 1. 调用 `BuildAsync(today)` |
| **预期** | 所有 Distribution 中时长总和 = 20 条记录的 duration_ms 总和 |
| **优先级** | P1 |

### E4-8 每日聚合正确性

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-008 |
| **模块** | DailyAggregationService |
| **标题** | 聚合后的 breakdown 合计等于原始数据 |
| **前置** | 插入 10 条覆盖所有类别和 work_tag 的记录 |
| **步骤** | 1. 调用 `AggregateAsync(today)`<br>2. 读取 daily_summaries |
| **预期** | app_breakdown 各项和 = domain_breakdown 各项和 = project_breakdown 各项和 = 原始数据 duration_ms 总和 |
| **优先级** | P0 |

### E4-9 周聚合正确性

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-009 |
| **模块** | WeeklyAggregationService |
| **标题** | 7 天数据的日均时长计算正确 |
| **前置** | 插入 7 天 daily_summaries，总时长 42000s |
| **步骤** | 1. 调用 `AggregateWeeklyAsync()` |
| **预期** | `avg_daily_hours` = 42000 / 7 / 3600 = 1.67h |
| **优先级** | P1 |

### E4-10 月聚合正确性

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-SR-010 |
| **模块** | MonthlyAggregationService |
| **标题** | 整月数据聚合 |
| **前置** | 插入 30 天 daily_summaries |
| **步骤** | 1. 调用 `AggregateMonthlyAsync()` |
| **预期** | 月总计 = 30 天总时长的合计 |
| **优先级** | P1 |

---

## 六、E5 性能基准测试

### E5-1 内存占用

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-PF-001 |
| **模块** | 全系统 |
| **标题** | 运行 30 分钟后内存占用 < 50MB |
| **前置** | 系统空闲状态 |
| **步骤** | 1. 启动 ActivityEngine<br>2. 模拟正常窗口活动（每 10s 切换一次）<br>3. 持续运行 30 分钟<br>4. 记录进程 Private Bytes<br>5. 停止引擎 |
| **预期** | 峰值内存 < 50MB；无明显内存增长趋势（无泄漏） |
| **基线** | < 50MB Private Bytes |
| **优先级** | P0 |

### E5-2 CPU 占用

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-PF-002 |
| **模块** | ActivityEngine |
| **标题** | 正常追踪时 CPU 平均占用 < 3% |
| **前置** | 系统空闲 |
| **步骤** | 1. 启动追踪引擎<br>2. 运行 10 分钟<br>3. 记录 Engine 进程 CPU 平均占用率 |
| **预期** | CPU 平均 < 3%；空闲降频后 < 1% |
| **优先级** | P0 |

### E5-3 批量写入性能

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-PF-003 |
| **模块** | ActivityEventRepository |
| **标题** | 批量插入 1000 条事件总耗时 < 500ms |
| **前置** | 数据库已初始化 |
| **步骤** | 1. 构造 1000 条 ActivityEvent<br>2. 启动计时器<br>3. 调用 `InsertBatchAsync(events)`<br>4. 停止计时器 |
| **预期** | 总耗时 < 500ms（即平均每条 < 0.5ms） |
| **优先级** | P1 |

### E5-4 日报查询性能

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-PF-004 |
| **模块** | 综合 |
| **标题** | 当天 500 条记录时时间线渲染 < 200ms |
| **前置** | 插入 500 条当天 activity_events |
| **步骤** | 1. 调用 `GetTodayEventsAsync()`<br>2. 构建 Timeline 数据模型 |
| **预期** | 数据查询 + 模型构建总耗时 < 200ms |
| **优先级** | P1 |

### E5-5 空闲降频验证

| 项目 | 内容 |
|------|------|
| **用例 ID** | TC-PF-005 |
| **模块** | ActivityEngine |
| **标题** | 空闲状态时轮询间隔从 2s 降至 10s |
| **前置** | 引擎正在运行 |
| **步骤** | 1. 记录当前 tick 间隔<br>2. 触发空闲<br>3. 等待 20s<br>4. 记录空闲时 tick 间隔 |
| **预期** | 正常间隔 = 2000ms；空闲间隔 ≈ 10000ms |
| **优先级** | P2 |

---

## 七、测试优先级定义

| 优先级 | 含义 | 测试通过要求 |
|--------|------|-------------|
| **P0** | 核心功能，阻断性 | 必须 100% 通过，否则不能合入 main |
| **P1** | 重要功能，非阻断 | 必须 100% 通过后才能发布 |
| **P2** | 辅助功能/边界场景 | 尽量通过，可暂缓 |

### P0 用例汇总（必须优先通过）

| ID | 描述 |
|----|------|
| TC-DB-001 | 数据库初始化创建所有表 |
| TC-DB-002 | 插入单条活动记录 |
| TC-DB-003 | 批量插入 50 条记录 |
| TC-DB-004 | 按日期范围查询 |
| TC-ENG-001 | 窗口切换检测 |
| TC-ENG-003 | 空闲触发 |
| TC-ENG-004 | 空闲恢复 |
| TC-ENG-006 | 睡眠事件 |
| TC-ENG-007 | 唤醒事件 |
| TC-ENG-011 | 引擎完整 tick 周期 |
| TC-ENG-012 | 引擎暂停/恢复 |
| TC-ENG-013 | 空闲延续逻辑 |
| TC-CA-001 | Chrome 标题解析 |
| TC-CA-002 | Edge 标题解析 |
| TC-CA-003 | Firefox 标题解析 |
| TC-CA-006 | VS Code 文件解析 |
| TC-CA-010 | 项目检测 Git 仓库 |
| TC-CA-013 | 分类器浏览器识别 |
| TC-CA-014 | 分类器编辑器识别 |
| TC-CA-017 | 关键词中英文混合 |
| TC-SR-001 | 按 App 聚合 |
| TC-SR-005 | 日报完整内容 |
| TC-SR-008 | 每日聚合正确性 |
| TC-PF-001 | 内存 < 50MB |
| TC-PF-002 | CPU < 3% |
